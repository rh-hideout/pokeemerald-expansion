# Milestone 1 Report — The Vertical Slice (2026-07-19)

**Status: complete.** All six build-order phases merged to `main`. The §7
slice is playable start-to-finish and verified headlessly: a new game opens
in the abandoned lab, the player places the campfire, Torchic comes home,
gets recruited to the Grove, and the overland walk to Route 103 reaches
every slice spot on foot — no wild battle can interrupt, because none exist.

## What was built (per phase, all gated on full suites)

1. **Condition engine** — all 20 spec §2 types as const data, NEGATE +
   OR-groups, met-masks for staged hints. `HABITAT_COND` authoring macro;
   designer reference at docs/habitat/authoring-conditions.md.
2. **Spot framework** — monotonic DORMANT→STIRRING→ACTIVE→BEFRIENDED,
   SaveBlock3 accessors, bg-sign-under-object inspectability, PLACE/OFFER
   verbs, event-driven recompute (map load / hour tick / weather / gives).
3. **Resident registry** — 96 individuals, RESIDENT_* conditions dex-backed
   (§5 overflow rule). **3.5** — v0.1-rev recast: type-berry economy
   (verified Sleep mapping), six slice spots, home-by-default rendering.
4. **The Grove** — 12-plot save array (3 rendered), §4 type-berry table,
   event-driven growth + RTC offline reconcile (single path, no double
   credit), six-out cap, recruit/rest/harvest flows, var-gfx workers.
5. **Lab opening** — §10 in-slice: furnishing items, per-(spot,condition)
   placement, three frames with offer-less auto-befriend ("come home"),
   lab spawn + hidden story cast, Machop's friendly bout, scavenge finds.
6. **Encounter cut + acceptance** — `HABITAT_NO_WILD_ENCOUNTERS` kills all
   wild rolls engine-wide (grass/surf, sweet scent, rock smash, fishing
   answers "not even a nibble"); scripted bouts unaffected. Full overland
   scenario circuit (below).

## Verification (all deterministic, screenshot evidence in verify-out/)

- `make verify` (lab): campfire placed → Torchic manifests + auto-befriends
  → recruit dismisses clean → walk to the Grove → Torchic tends its plot →
  plot answers "Growing: Leppa".
- `--scenario skitty`: full overland walk (lab → Littleroot → Route 101 →
  Oldale → Route 103 west bank) → place doll → Skitty manifests → offer
  Persim → BEFRIENDED. The Route 101 tall-grass crossing is a live proof of
  the encounter gate.
- `--scenario machop`: overland walk → staged ACTIVE hint → bout offer
  declined → scavenge find picked up (proven positionally: the player ends
  standing on the collected ball's tile).
- Native: 49 Habitat tests (conditions, spots, state machine, manager,
  grove, residents, encounter law). Full upstream suite green. Machop's
  bout win→BATTLE_WIN→auto-befriend and all Grove growth/harvest logic are
  native-tested (RTC-hour scenarios stay native; the runner has no
  clock-warp op — noted for M2 if in-game growth proof is wanted).
- EWRAM 86.75% (227,400 / 262,144 B) — flat across M1; all habitat state
  lives in SaveBlock3 (820/1,624 B) and ROM.

## Discoveries that changed the map (flagged for your renovation pass)

- **The phase-2 slice cluster was on Route 103's east bank — which is open
  ocean and Surf-only land.** The old slice spawn had been warping the
  player directly onto water, so everything "worked" until the lab spawn
  made the player walk there. All slice content (6 spots + signs, the three
  vanilla berry trees, 3 finds) now sits on decoded-walkable west-bank
  tiles; berry-tree ids 5–7 are unchanged so zone-1 conditions held without
  data edits. Coordinates were always my placeholder placement — your §3
  renovation pass owns the final staging.
- **Engine bug found by the walk:** map-connection crossings skipped the
  habitat map-load recompute, so walked-into maps spawned spots with stale
  visibility (a dormant Skitty stood visible). Fixed at the connection-load
  path; warp-based testing could never have caught it.
- **Slice init now neutralizes the vanilla overland:** Littleroot's
  "can't leave without a Pokémon" force-walk, Route 101's Birch rescue and
  its passage-blocking youngster, Route 103's rival/Birch/wandering boy,
  Oldale's rival object. All hidden/pre-set with the abandoned fiction.

## Open questions (batched; placeholders are loud and working)

1. **Lab frame↔species binding when the choice is free.** I pre-bound
   campfire→Torchic, plant→Treecko, basin→Mudkip — exact for the §10
   dev-pin. If the real intent is "any element item at any frame decides
   that frame's starter," the placement engine supports it but the spot
   table needs a binding model from you.
2. **Who battles the Machop bout.** Placeholder: the first resident steps
   up as a borrowed level-50 battler; refusal text when no residents exist.
   Party layer is untouched per the deferral.

## Deferred (tracked, not blocking)

- §9 anti-regret line-matching for RESIDENT_SPECIES + registry work
  counter (+1 B/resident) — lands with evolution rituals.
- Zone filter for resident conditions; Route 103 exceeds the §3 map-budget
  law (vanilla NPC crowd) — renovation pass.
- Runner clock-warp op if in-game growth/night scenarios are wanted
  (Heracross night chain + growth stages are native-proven today).
